# ThriftyChef — core Python dependencies (Phase 0–4)
# Install: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

# Data & numerics
pandas>=2.0
numpy>=1.24
scipy>=1.11

# ML & recommender systems
scikit-learn>=1.3
scikit-surprise>=1.1.3
gensim>=4.3

# Deep learning (Mult-VAE extension — optional until Phase 2.5)
torch>=2.0

# Retrieval (Phase 5+)
faiss-cpu>=1.7.4

# API (Phase 5+)
fastapi>=0.109
uvicorn[standard]>=0.27
httpx>=0.26
python-dotenv>=1.0
psycopg2-binary>=2.9
supabase>=2.0

# Viz & notebooks
matplotlib>=3.8
seaborn>=0.13
jupyter>=1.0
ipykernel>=6.29

# Utils
tqdm>=4.66
python-dateutil>=2.8
openpyxl>=3.1

# Testing
pytest>=8.0

# Explainability (lecture extension — SHAP bonus)
shap>=0.44
